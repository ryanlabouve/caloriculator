import { percents } from '../../../helpers/percents';
import { module, test } from 'qunit';

module('Unit | Helper | percents');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = percents(42);
  assert.ok(result);
});
